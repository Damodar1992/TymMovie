# Neon PostgreSQL (required for serverless API on Vercel)
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# The Movie DB API key (Bearer token, for metadata enrichment)
TMDB_API_KEY=your_tmdb_bearer_token
