# Neon PostgreSQL (browser-accessible connection string for Neon serverless driver)
VITE_DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# The Movie DB API key (Bearer token or API key, for metadata enrichment)
VITE_TMDB_API_KEY=your_tmdb_bearer_token
